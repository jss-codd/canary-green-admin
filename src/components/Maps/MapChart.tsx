
import React, { memo, useState } from "react";
import {
  ZoomableGroup,
  ComposableMap,
  Geographies,
  Geography
} from "react-simple-maps";

const MapChart = () => {
  return (
    <>
    &nbsp;
    </>
  );
};

export default memo(MapChart);